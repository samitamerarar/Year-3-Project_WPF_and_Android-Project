<UserControl x:Class="Prototype_Heacy_client.Views.UserControls.UserControl_Hint"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Prototype_Heacy_client.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
   
    
    <StackPanel Margin="30 0 20 0">
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>

            <Label Margin="0 18 0 0" Name="hint" Grid.Column="0" FontWeight="Bold" HorizontalAlignment="left" VerticalAlignment="Top"  Foreground="White" FontSize="14"/>
            <Border BorderBrush="LightGray" Margin="0,10" Grid.Column="1" BorderThickness="1">
                <TextBox Margin="0" Padding="10 0" Name="HintContent"  Text="{Binding User.Username}"   Style="{StaticResource MaterialDesignFloatingHintTextBox}" Foreground="White" FontFamily="Champagne &amp; Limousines" FontSize="14"/>
            </Border>
        </Grid>
    </StackPanel>

</UserControl>
