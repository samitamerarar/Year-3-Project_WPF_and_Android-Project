<UserControl x:Class="Prototype_Heacy_client.Views.UserControls.UserControl_PlayerTournament"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Prototype_Heacy_client.Views.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0">
            <TextBlock Padding="0" Margin="00" RenderTransformOrigin="0.5,0.5"  Height="45">
                <TextBlock.Background>
                    <ImageBrush ImageSource="{Binding ProfilePic, Mode=OneWay, UpdateSourceTrigger=PropertyChanged}" Stretch="UniformToFill"/>
                </TextBlock.Background>
            </TextBlock>
        </Border>
        <Border Grid.Column="1">
            <TextBlock x:Name="Player" Background="#FF2E7FC3" VerticalAlignment="Center" Height="45" Padding="15 13 15 11" Foreground="Black" FontSize="16" Margin="0 0 0 2" />
        </Border>
    </Grid>
</UserControl>
